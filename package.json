{
  "name": "sdk-orchestrator",
  "version": "0.1.0",
  "description": "SLE (Sustained Learning Engine) - SDK orchestration system",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "test": "find tests -name '*.test.ts' | sort | xargs npx tsx --test",
    "test:watch": "find tests -name '*.test.ts' | sort | xargs npx tsx --watch --test",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "zod": "^3.22.0",
    "js-yaml": "^4.1.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "@types/js-yaml": "^4.0.5",
    "typescript": "^5.0.0",
    "tsx": "^4.19.0"
  },
  "keywords": [
    "sle",
    "orchestration",
    "development-automation"
  ],
  "engines": {
    "node": ">=20.0.0"
  }
}
